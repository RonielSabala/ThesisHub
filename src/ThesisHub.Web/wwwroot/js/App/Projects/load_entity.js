function fillEntityField(entity) {
    $title.text(entity.title);
    $projectDescription.text(entity.projectDescription);
    $registrationDate.text(entity.registrationDate);
    $projectStatus.text(entity.projectStatus);
    $student.text(entity.studentName);
}
